{{#if canJoinGroup}}
  {{d-button action=(action "joinGroup")
      class="group-index-join"
      icon="user-plus"
      label="groups.join"
      disabled=updatingMembership}}
{{else if canLeaveGroup}}
  {{d-button action=(action "leaveGroup")
      class="btn-danger group-index-leave"
      icon="user-times"
      label="groups.leave"
      disabled=updatingMembership}}
{{else if canJoin}}
  {{#if canAccessByVerifiedCredentials}}
    {{d-button action=(action "verifyCredentials")
        class="group-verify-request btn-primary"
        disabled=loading
        icon="passport"
        label="verifiable_credentials.button"}}
  {{/if}}
  {{#if canRequestMembership}}
    {{d-button action=(action "showRequestMembershipForm")
        class="group-index-request"
        disabled=loading
        icon="user-plus"
        label="groups.request"}}
  {{/if}}
{{else}}
  {{yield}}
{{/if}}